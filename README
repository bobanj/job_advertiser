Job Advertiser - It's a simple app for managing jobs
======================================================
* Currently adapted only for dev env.

Install
======================================================
* bundle install
* run migrations
* change settings in config/initializers/configuration.rb
* rails server

